package com.example.wetok.searchTree;

import com.example.wetok.bean.Post;
import com.example.wetok.parserAndTokenizer.Exp;

import java.util.List;

public class Query {
    //TODO search results in different condition
    // two cases:
    // 1. intersection of two post list
    // 2. union of two post list
    public static List<Post> query(List<Post> plist, Search s, Exp condition){

        return null;
    }

}
